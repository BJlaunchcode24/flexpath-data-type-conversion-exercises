// Create a JavaScript object
const student = {
    name: "Blerta",
    age: 34,
    enrolled: true,
    courses: ["JavaScript", "React", "Spring Boot"]
  };
  
  // Convert the object to a JSON string
  const jsonString = JSON.stringify(student);
  
  // Log the JSON string to the console
  console.log(jsonString);
  